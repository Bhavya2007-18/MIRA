export { default, useLocationTracker, LOCAL_IP, BACKEND_TRACKING_URL, TRACKING_INTERVAL_MS } from '../src/hooks/useLocationTracker';
export type { LocationCoordinates, UseLocationTrackerResult } from '../src/hooks/useLocationTracker';
